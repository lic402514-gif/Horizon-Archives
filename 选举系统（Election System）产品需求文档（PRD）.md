# 选举系统（Election System）产品需求文档（PRD）

## 1. 产品概述

### 1.1 产品名称

选举系统（Election System）

### 1.2 所属模块

公共治理平台

系统组成：

- 议题系统（Issue）
- 观点卡系统（Opinion Card）
- 投票系统（Voting）
- 选举系统（Election）
- 任命系统（Appointment）
- 决议系统（Resolution）

### 1.3 产品定位

选举系统用于组织和管理平台内部的正式选举活动。

系统支持：

- 管理员发起选举；
- 用户参与候选人提名；
- 候选人展示个人资料和竞选声明；
- 用户按照指定规则投票；
- 系统进行计票和生成选举报告；
- 管理员根据选举结果进行最终任命。

选举结果本身代表用户意见和推荐结果，不自动修改用户权限或产生职务。

------

# 2. 设计原则

## 2.1 选举与任命分离

选举产生：

- 推荐结果；
- 民意排序；
- 候选人支持情况。

任命产生：

- 实际职位；
- 权限变化；
- 管理身份。

流程：

管理员发起选举

↓

用户投票

↓

生成选举结果

↓

管理员审议

↓

管理员任命

------

## 2.2 选举过程不可随意修改

选举开始后：

- 候选名单冻结；
- 投票规则冻结；
- 投票记录不可修改；
- 关键操作必须记录日志。

------

## 2.3 过程公开透明

系统需要保存：

- 发起记录；
- 阶段变化记录；
- 投票统计；
- 计票过程；
- 结果报告。

------

# 3. 用户角色

## 3.1 超级管理员

权限：

- 创建选举；
- 设置选举规则；
- 开启和关闭阶段；
- 审核候选人；
- 发起计票；
- 发布选举报告；
- 根据结果进行任命。

------

## 3.2 普通用户

权限：

- 查看选举信息；
- 提名候选人；
- 提交候选申请；
- 查看候选人资料；
- 投票；
- 查看结果。

------

## 3.3 候选人

权限：

- 编辑个人介绍；
- 发布竞选声明；
- 回复选民问题；
- 查看自己的支持情况。

------

# 4. 选举生命周期

选举采用状态机管理。

状态：

## 4.1 草稿阶段（Draft）

管理员创建选举。

可修改：

- 选举名称；
- 选举说明；
- 投票方式；
- 时间；
- 职位；
- 候选资格。

用户不可见。

------

## 4.2 提名阶段（Nomination）

管理员开启提名。

功能：

- 用户提交候选申请；
- 用户推荐候选人；
- 候选人填写资料。

状态：

Nomination

------

## 4.3 候选审核阶段（Review）

管理员审核候选资格。

审核内容：

- 身份；
- 资格；
- 信息完整性。

候选状态：

- 待审核；
- 通过；
- 拒绝。

------

## 4.4 投票阶段（Voting）

正式投票。

系统冻结：

- 候选名单；
- 投票方式；
- 席位数量；
- 投票资格。

允许：

- 用户投票；
- 查看候选人信息。

------

## 4.5 计票阶段（Counting）

投票结束。

系统：

- 锁定选票；
- 执行算法；
- 生成结果。

------

## 4.6 结果公布阶段（Published）

公开：

- 投票人数；
- 投票率；
- 候选人排名；
- 最终结果。

生成：

选举报告（Election Report）

------

## 4.7 归档阶段（Archived）

保存历史记录。

不可修改。

------

# 5. 选举创建功能

管理员创建选举时填写：

## 基础信息

字段：

- 选举名称
- 选举简介
- 选举目的
- 发起人
- 创建时间

示例：

2026年度图书馆委员会委员推荐选举

------

## 职位信息

字段：

- 职位名称；
- 席位数量；
- 职责说明。

例如：

职位：

图书馆委员会委员

席位：

5

------

## 投票规则

支持：

- 简单多数制；
- 绝对多数制；
- 批准投票；
- STV单票可转移制。

------

# 6. 候选人系统

## 6.1 候选人资料

字段：

- 姓名；
- 用户ID；
- 头像；
- 个人简介；
- 所属组织；
- 经历。

------

## 6.2 竞选声明

候选人可以发布：

- 竞选目标；
- 政策主张；
- 工作计划。

结构：

标题：

正文：

附件：

引用资料：

------

## 6.3 候选人展示页面

包含：

- 基础资料；
- 竞选声明；
- 支持人数；
- 历史记录。

------

# 7. 投票系统

## 7.1 投票资格

系统根据规则确定：

- 哪些用户可以投票；
- 是否允许匿名；
- 是否限制重复投票。

------

## 7.2 投票记录

每次投票保存：

- 用户；
- 选举；
- 时间；
- 投票内容；
- 校验信息。

------

# 8. 投票方式支持

## 8.1 简单多数制

用户选择一个候选人。

结果：

票数最高者胜出。

适用于：

- 单一职位；
- 简单推荐。

------

## 8.2 绝对多数制

候选人需要获得：

超过50%的有效票。

如果无人达到：

进入：

- 第二轮投票；
   或
- 按规则处理。

------

## 8.3 批准投票

用户可以支持多个候选人。

统计：

每个候选人的支持次数。

适用于：

多人推荐。

------

## 8.4 STV单票可转移制

用户按照偏好排序：

第一选择：

第二选择：

第三选择：

系统根据规则：

- 淘汰低票候选人；
- 转移选票；
- 计算最终席位。

适用于：

多席位选举。

------

# 9. 选举结果系统

## 9.1 结果数据

保存：

- 候选人排名；
- 获得票数；
- 支持比例；
- 是否进入推荐名单。

------

## 9.2 选举报告

系统生成正式报告。

内容：

## 基本信息

- 选举名称；
- 时间；
- 发起人；
- 投票方式。

## 投票情况

- 符合资格人数；
- 实际投票人数；
- 投票率；
- 有效票；
- 无效票。

## 候选人结果

展示：

|排名|候选人|票数|比例|

------

## 计票过程

根据算法展示：

- 计算规则；
- 轮次；
- 淘汰过程；
- 最终结果。

------

## 最终决定

显示：

- 推荐结果；
- 管理员任命结果。

------

# 10. 任命关联

选举结束后：

管理员可以创建任命记录。

任命信息：

- 任命人员；
- 职位；
- 任命时间；
- 任命理由；
- 生效时间。

选举结果不会自动改变权限。

------

# 11. 数据库设计

## elections（选举表）

字段：

- id
- title
- description
- status
- voting_method
- start_time
- end_time
- creator_id
- created_at

------

## candidates（候选人表）

字段：

- id
- election_id
- user_id
- name
- biography
- manifesto
- status

------

## ballots（选票表）

字段：

- id
- election_id
- voter_id
- created_at

------

## ballot_choices（投票选择表）

字段：

- ballot_id
- candidate_id
- rank

支持：

- 单选；
- 批准投票；
- STV排序。

------

## election_results（选举结果表）

字段：

- election_id
- candidate_id
- votes
- ranking
- recommended

------

## election_reports（选举报告表）

字段：

- election_id
- summary
- statistics
- result_data
- generated_time

------

## election_logs（选举日志表）

字段：

- election_id
- operator_id
- action
- before_status
- after_status
- reason
- timestamp

------

# 12. SSG生成方案

在选举结算后，选举报告由于系统采用静态页面生成并且公示到用户界面：

选举页面：

```
/elections/{id}/
```

包含：

- 选举名称、选举时间、选举规则、选举用户（哪个超管发起的）、选民统计（包含实际投票人数与有效选民）、选举卡id（比如ELE-2026-7-17）、候选位、选举人得票情况、条形统计图（候选人-票数统计图）

报告页面：

```
/elections/{id}/report/
```

选举结算后立即生成。

------

# 13. 安全要求

## 投票安全

必须保证：

- 一个用户只能投一次；
- 投票记录不可篡改；
- 管理员不能修改用户选票。

------

## 权限安全

管理员操作：

必须记录：

- 操作者；
- 时间；
- 操作内容；
- 原因。

------

## 审计要求

所有关键事件保存：

- 创建；
- 开启；
- 暂停；
- 结束；
- 计票；
- 发布。

------

# 14. MVP版本范围

实现：

- 创建选举；
- 候选人管理；
- 用户投票；
- 简单多数计票；
- 结果页面。

- 批准投票；
- 绝对多数；
- STV；
- 候选人竞选声明；
- 选举报告。

------

# 总结

选举的过程为：超级管理员发起选举，选举阶段如下：
 → 用户参与提名
 → 候选人展示主张
 → 用户投票
 → 系统计票
 → 生成选举报告

选举界面用时间轴直观地列举选举阶段，并且要求在页面标注当前阶段。要求生成的选举界面统一放在/dist目录下的独立文件夹下。在网页内用户界面则将全部的选举界面的超链接卡片放在一个专门存放和用于导航的页面之下，页面布局要求按照时间顺序依次存排列。便于用户导航

系统最终形成可追溯、可审计、可归档的公共决策记录。